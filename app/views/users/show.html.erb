<h3>User</h3>
<p>Name: <%= @user.name if @user.name %></p>
<p>Email: <%= @user.email if @user.email %></p>

<h4>Recipes</h4>
<ul>
  <% @recipes.each do |recipe| %>
    <li><%= link_to recipe.title, recipe %> from <%= recipe.created_at %> </li>
  <% end %>
</ul>
